Config = {}

Config.sal_spawnmenu = false -- Enable this if using the sal_spawnmenu resource.

Config.defaultSpawn = { -- Change these if not using the spawn menu. Creates a default location for spawning players. 
    x = 165.180,  
    y = -984.785,
    z = 30.092
}